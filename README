This code is in development.

Dependencies (exemple with Debian packages):
"python3-dev", "python3-gi", "libasound-dev", "libjack-dev", "make", "gcc"
(and "python3-stdeb" for debian package source development)

For a quick test use "./test_midilooper.sh".

Type "make help" for quick usage. Or move to src directory and use "./setup.py"
to build or package the project (see "./setup.py --help" and Python Distutils
for more information).

For old jack api, the environment variable OLDJACKAPI must be set.
ex: export OLDJACKAPI=yes
